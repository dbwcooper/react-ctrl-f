# react-ctrl-f

Using react to realize Ctrl + F serach function

## Usage

```js
import {
  MatchText,
  SearchProvider,
  SearchContext,
  SearchEventContext,
} from '../components';
```

see `src/pages/Home.tsx` and `src/pages/Search.tsx`
